function logArrayElements(element, index, array) {
	//	  console.log('a[' + index + '] = ' + element);
	console.log(array);

}
// Note ellision, there is no member at 2 so it isn't visited
 [2, 5,0 , 9].forEach(logArrayElements);
// // logs:
// // a[0] = 2
// // a[1] = 5
// // a[3] = 9
